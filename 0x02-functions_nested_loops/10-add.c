#include "main.h"
/**
 * add - add two integers and returns the result
 * Return: result
 * @num1: first number
 * @num2: second number
 */
int add(int num1, int num2)
{
	int result;

	result = num1 + num2;
	return (result);
}
