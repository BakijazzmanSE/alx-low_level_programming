#include "main.h"
/**
 * _abs - computes the absolute value of an integer
 * Return: 0 success
 * @num: the absolute value
 */
int _abs(int num)
{
	if (num > 0)
	{
		return (num);
	}

	return (-num);
}
