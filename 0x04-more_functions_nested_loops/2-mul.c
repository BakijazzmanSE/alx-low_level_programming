#include "main.h"
/**
 * mul - a function that multiplies two integers
 * @a: the first integer
 * @b: the second integer
 * Return: the multiplied value
 */
int mul(int a, int b)
{
	mul = a * b;

	return (mul);
}
