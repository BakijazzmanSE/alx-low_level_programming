#include <stdio.h>
/**
 * main - prints the number of arguement passed into it
 * @argc: count
 * @argv: value
 * Return: success
 */
int main(int argc, char *argv[])
{
	(void)argv;
	printf("%d\n", argc);
	return (0);
}
