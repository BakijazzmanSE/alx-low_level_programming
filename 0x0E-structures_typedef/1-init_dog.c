#include <stdio.h>
#include "main.h"
/**
 * init_dog - function that initiates the variable of type struct
 * @d: the pointer to the struct
 * @name: name of dog
 * @age: age of dog
 * @owner: owner of dog
 * Return: void
 */
void init_dog(struct dog *d, char *name, float age, char *owner)
{
	if (d)
	{
		char *name = name;
		float age = age;
		char *owner = owner;
	}
}
